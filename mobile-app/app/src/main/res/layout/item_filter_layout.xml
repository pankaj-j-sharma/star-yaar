<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginStart="10dp"
    android:layout_marginEnd="10dp"
    android:padding="10dp">

    <androidx.cardview.widget.CardView
        android:id="@+id/cardvidewfilter"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="5dp"
        android:layout_marginRight="10dp"
        android:background="@drawable/camera_filter_background"
        app:cardCornerRadius="10dp">

        <jp.co.cyberagent.android.gpuimage.GPUImageView
            android:id="@+id/iv_photo"
            android:layout_width="90dp"
            android:layout_height="90dp"
            app:gpuimage_show_loading="false"
            app:gpuimage_surface_type="texture_view"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />
    </androidx.cardview.widget.CardView>

    <TextView
        android:id="@+id/filter_txt"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginTop="4dp"
        android:elevation="5dp"
        android:gravity="center"
        android:maxLines="2"
        android:paddingTop="2dp"
        android:textAlignment="center"
        android:textAllCaps="false"
        android:textColor="@color/white"
        android:textSize="6sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="@+id/cardvidewfilter"
        app:layout_constraintStart_toStartOf="@+id/cardvidewfilter"
        app:layout_constraintTop_toBottomOf="@+id/cardvidewfilter"
        tools:text="" />
</androidx.constraintlayout.widget.ConstraintLayout>